## Packages
recharts | For visualizing task completion statistics
date-fns | Robust date formatting and calculations
framer-motion | Smooth animations for task interactions and page transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
Using Replit Auth for authentication.
Frontend assumes 401 response means "redirect to login".
Mobile-first layout with bottom navigation on small screens.
